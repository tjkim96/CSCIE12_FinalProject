---
const { title = "Coffee" } = Astro.props;
const path = Astro.url.pathname;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>

  <body>
    <header>
      <div class="logo">{title}</div>

      <!-- Make toolbar -->
      <nav class="toolbar" aria-label="Main navigation">
        <a
          class="tab"
          href="/"
          aria-current={path === "/" ? "page" : undefined}
        >
          Home
        </a>

        <a
          class="tab"
          href="/countries"
          aria-current={path.startsWith("/countries") ? "page" : undefined}
        >
          Browse Coffees By Country
        </a>
      </nav>
    </header>

    <main>
      <slot />
    </main>

    <footer>
      This work is part of the Final Project for the course CSCI E-12, Fall
      2025, taught at
      <a href="https://extension.harvard.edu/"
        >Harvard University Extension School</a
      >
      by David Heitmeyer. Authored by Jaisohn Kim.
    </footer>
  </body>
</html>

<style>
  :root {
    --latte: #fbf6ef;
    --cream: #fff7ee;
    --paper: #ffffff;
    --espresso: #2b1b12;
    --mocha: #6f4e37;
    --cocoa: #8b5e3c;
    --border: #ead7c3;
    --shadow: 0 10px 24px rgba(0, 0, 0, 0.08);
  }

  * {
    box-sizing: border-box;
  }

  body {
    font-family:
      ui-sans-serif,
      system-ui,
      -apple-system,
      Segoe UI,
      Roboto,
      Arial,
      sans-serif;
    margin: 0;
    background: var(--latte);
    color: var(--espresso);
  }

  header,
  main,
  footer {
    margin-left: 4rem;
    margin-right: 4rem;
  }

  header {
    padding-top: 2rem;
  }

  header .logo {
    font-size: 3rem;
    font-weight: 800;
    letter-spacing: -0.02em;
    line-height: 1.1;
    margin-bottom: 1.25rem;
    color: var(--espresso);
  }

  /* Toolbar */
  .toolbar {
    background: linear-gradient(180deg, var(--mocha), #5d3f2c);
    padding: 0.85rem 1.1rem;
    display: flex;
    gap: 2rem;
    align-items: center;
    border-radius: 14px;
    box-shadow: var(--shadow);
  }

  .toolbar .tab:link,
  .toolbar .tab:visited {
    color: #fff;
    text-decoration: none;
    font-size: 1.05rem;
    padding: 0.25rem 0.1rem;
  }

  .toolbar .tab:hover {
    text-decoration: underline;
  }

  .toolbar .tab[aria-current="page"] {
    font-weight: 800;
    text-decoration: underline;
  }

  /* Content + footer links */
  main a:link,
  main a:visited,
  main a:hover,
  main a:active,
  footer a:link,
  footer a:visited,
  footer a:hover,
  footer a:active {
    text-decoration: none;
    color: var(--mocha);
  }

  main a:hover,
  footer a:hover {
    text-decoration: underline;
  }

  footer {
    margin-top: 4rem;
    border-top: 1px solid var(--border);
    padding-top: 2rem;
    padding-bottom: 2rem;
    color: #4a3426;
  }

  @media (max-width: 700px) {
    header,
    main,
    footer {
      margin-left: 1rem;
      margin-right: 1rem;
    }

    header .logo {
      font-size: 2rem;
    }

    .toolbar {
      flex-wrap: wrap;
      gap: 1rem;
    }
  }
</style>
